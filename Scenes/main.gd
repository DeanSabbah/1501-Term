extends Node2D

@onready var industrial:TileMap = $Industrial
@onready var power:TileMap = $Power_Station
@onready var exclusion:TileMap = $Exclusion

# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass